# Optional live-test environment only.
# Do not store real credentials in this file.
# Copy to .env locally if you need shell helpers, but the app itself reads
# Alibaba credentials from macOS Keychain via scripts/configure-aliyun-credentials.sh.

ALIBABA_CLOUD_ACCESS_KEY_ID=
ALIBABA_CLOUD_ACCESS_KEY_SECRET=
