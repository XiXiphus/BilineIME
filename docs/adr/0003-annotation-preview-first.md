# ADR 0003: Use IMKCandidates Annotation Before Custom UI

## Status

Accepted

## Decision

Mode 1 uses `IMKCandidates` plus annotation preview before any custom bilingual candidate panel.

## Rationale

The stock candidate path validates interaction design with lower platform risk and fewer rendering variables.
